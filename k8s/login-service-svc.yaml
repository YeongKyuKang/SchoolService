apiVersion: v1
kind: Service
metadata:
  name: login-service-service
  labels:
    app: login-service
spec:
  selector:
    app: login-service
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 5006
      nodePort: 30000
  type: NodePort