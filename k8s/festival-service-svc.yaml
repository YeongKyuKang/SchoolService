apiVersion: v1
kind: Service
metadata:
  name: festival-service-svc
  labels:
    app: festival-service
  namespace: default
spec:
  selector:
    app: festival-service
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 5002
  type: LoadBalancer