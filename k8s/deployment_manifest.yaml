apiVersion: apps/v1
kind: Deployment
metadata:
  name: festival-service-container
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: festival-service
  template:
    metadata:
      labels:
        app: festival-service
    spec:
      containers:
      - name: festival-service-container
        image: ghcr.io/yeongkyukang/festival_service:latest
        env:
          - name: JWT_SECRET_KEY
            valueFrom:
              secretKeyRef:
                name: jwt-secret
                key: JWT_SECRET_KEY
          
          - name: DB_WRITE_HOST
            value: "121.160.41.222"
          - name: DB_WRITE_PORT
            value: "3306"

          - name: DB_READ_HOSTS
            value: "rds-s1.c9ywksucaj8v.ap-northeast-2.rds.amazonaws.com,rds-s2.c9ywksucaj8v.ap-northeast-2.rds.amazonaws.com"
          - name: DB_READ_PORT
            value: "3306"

          - name: DB_USER
            value: admin
          - name: DB_PASSWORD
            value: "P*ssword123"
          - name: DB_NAME
            value: festival_db
        ports:
          - containerPort: 5002
      imagePullSecrets:
      - name: ghcr-secret
